micronaut:
  application:
    name: keyManagerGrpc

datasources:
  default:
    url: ${DB_URL:`jdbc:mysql://localhost:3306/key_manager_grpc`}
    username: zup5540
    password: Adm@1234

jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: update